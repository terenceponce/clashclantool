- unless flash.empty?
  - flash.each do |key, value|
    .alert.alert-dismissable{class: flash_class(key), role: 'alert'}
      %button.close{type: 'button', data: {dismiss: 'alert'}, aria: {label: 'Close'}}
        %span{aria: {hidden: 'true'}} &times;
      = value
